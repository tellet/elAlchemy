WindowManager:
    MainWindow:
    IngredientsOnlyWindow:
    IngredientsSelectionWindow:
    EffectsOnlyWindow:
    BothEffectsWindow:
    BothIngredientsWindow:
    NoCocktailsWindow:
    EffectsDictionaryWindow:
    HealDictionaryWindow:


<WindowManager>:
    id: screen_manager
    name: "screen_manager"

    MainWindow:
        id: main_window
        name: "main_window"
        manager: screen_manager

        BoxLayout:
            name: "1"
            orientation: "horizontal"
            size_hint: 1, .33
            pos_hint: {"top":1.0}
            Button:
                id: main_ings_only_btn
                text: "Знаю только ингредиенты"
                size_hint: .5, 1
                pos_hint:{"top":1.0, "left":1.0}
                font_size: 24
                on_release:
                    app.root.current = "ingredients_only_window"
                    app.root.transition.direction = 'left'
            Button:
                id: main_both_btn
                text: "Знаю эффекты и ингредиенты"
                size_hint: .5, 1
                pos_hint:{"top":1.0, "right":1.0}
                font_size: 24
                on_release:
                    app.root.current = "both_effects_window"
                    app.root.transition.direction = 'left'

        BoxLayout:
            name: "2"
            orientation: "horizontal"
            size_hint: 1, .33
            pos_hint:{"center_y":0.5}
            Button:
                id: main_effects_only_btn
                text: "Знаю только эффекты"
                size_hint: .5, 1
                pos_hint:{"top":1.0, "left":1.0}
                font_size: 24
                on_release:
                    app.root.current = "effects_only_window"
                    app.root.transition.direction = 'left'

            Button:
                id: main_exit_btn
                text: "Выйти"
                size_hint: .5, 1
                pos_hint:{"top":1.0, "right":1.0}
                font_size: 24
                background_color: (1, 2, 1, 3)
                on_press: quit()
        BoxLayout:
            name: "3"
            orientation: "horizontal"
            size_hint: 1, .33
            pos_hint: {"bottom":1.0}
            Button:
                id: main_test_btn
                text: "Лечение болезней"
                size_hint: .5, 1
                pos_hint:{"top":1.0, "left":1.0}
                font_size: 24
                background_color: (1, 2, 1, 3)
                on_press:
                    app.root.current = "heal_dictionary_window"
                    app.root.transition.direction = 'left'

            Button:
                id: main_effects_dict_btn
                text: "Описание эффектов"
                size_hint: .5, 1
                pos_hint:{"top":1.0, "right":1.0}
                font_size: 24
                background_color: (1, 2, 1, 3)
                on_press:
                    app.root.current = "effects_dictionary_window"
                    app.root.transition.direction = 'left'

    EffectsOnlyWindow:
        id: effects_only_window
        name: "effects_only_window"
        manager: screen_manager
        GridLayout:
            id: effects_only_root_grid
            cols:1
            Button:
                id: both_back_btn
                text: "На Главную"
                size_hint: 1, .1
                pos_hint:{"bottom":1.0}
                background_color: (1, 2, 1, 3)
                on_release:
                    app.root.current = "main_window"
                    app.root.transition.direction = 'right'

    IngredientsOnlyWindow:
        id: ingredients_only_window
        name: "ingredients_only_window"
        manager: screen_manager
        GridLayout:
            id: ings_only_root_grid
            cols:1
            Button:
                id: both_back_btn
                text: "На главную"
                size_hint: 1, .1
                pos_hint:{"bottom":1.0}
                background_color: (1, 2, 1, 3)
                on_release:
                    app.root.current = "main_window"
                    app.root.transition.direction = 'right'

    IngredientsSelectionWindow:
        id: ingredients_selection_window
        name: "ingredients_selection_window"
        manager: screen_manager
        GridLayout:
            id: ings_selection_root_grid
            cols:1
            Button:
                id: ings_selection_back_btn
                text: "Назад к эффектам"
                size_hint: 1, .1
                pos_hint:{"bottom":1.0}
                background_color: (1, 2, 1, 3)
                on_release:
                    app.root.current = "ingredients_only_window"
                    app.root.transition.direction = 'right'

    BothEffectsWindow:
        id: both_effects_window
        name: "both_effects_window"
        manager: screen_manager
        GridLayout:
            id: both_effects_root_grid
            cols:1
            Button:
                id: both_effects_back_btn
                text: "На Главную"
                size_hint: 1, .1
                pos_hint:{"bottom":1.0}
                background_color: (1, 2, 1, 3)
                on_release:
                    app.root.current = "main_window"
                    app.root.transition.direction = 'right'
            Button:
                id: both_effects_ings_btn
                text: "К выбору ингредиентов"
                size_hint: 1, .1
                pos_hint:{"bottom":1.0}
                background_color: (1, 2, 1, 3)
                on_release:
                    app.root.current = "both_ings_window"
                    app.root.transition.direction = 'left'


    BothIngredientsWindow:
        id: both_ings_window
        name: "both_ings_window"
        manager: screen_manager
        GridLayout:
            id: both_ings_root_grid
            cols:1
            Button:
                id: both_ings_back_btn
                text: "На Главную"
                size_hint: 1, .1
                pos_hint:{"bottom":1.0}
                background_color: (1, 2, 1, 3)
                on_release:
                    app.root.current = "main_window"
                    app.root.transition.direction = 'right'

    NoCocktailsWindow:
        id: no_cocktails_window
        name: "no_cocktails_window"
        manager: screen_manager
        GridLayout:
            id: no_cocktails_root_grid
            cols:1
            Label:
                text: "Упс, нет таких зелий"
                font_size: 24
            Image:
                source: "images/cat_logo.png"
            Button:
                id: both_ings_back_btn
                text: "На Главную"
                size_hint: 1, .1
                pos_hint:{"bottom":1.0}
                background_color: (1, 2, 1, 3)
                on_release:
                    app.root.current = "main_window"
                    app.root.transition.direction = 'right'

    EffectsDictionaryWindow:
        id: effects_dictionary_window
        name: "effects_dictionary_window"
        manager: screen_manager
        GridLayout:
            id: effects_dict_root_grid
            cols:1
            Button:
                id: effects_dict_back_btn
                text: "На Главную"
                size_hint: 1, .1
                pos_hint:{"bottom":1.0}
                background_color: (1, 2, 1, 3)
                on_release:
                    app.root.current = "main_window"
                    app.root.transition.direction = 'right'

    HealDictionaryWindow:
        id: heal_dictionary_window
        name: "heal_dictionary_window"
        manager: screen_manager
        GridLayout:
            id: heal_dict_root_grid
            cols:1
            Button:
                id: heal_dict_back_btn
                text: "На Главную"
                size_hint: 1, .1
                pos_hint:{"bottom":1.0}
                background_color: (1, 2, 1, 3)
                on_release:
                    app.root.current = "main_window"
                    app.root.transition.direction = 'right'